<tool id="quick_snpeff" name="SNP Effect Assessment">
	<command> </command>
	<inputs>
		<param name="refseq" type="data" format="fasta" label="Reference genome" />
		<param name="annotation" type="data" format="gff" label="Annotation file" help="Current only GFF format are tested" />
		<param name="varfile" type="data" format="vcf" label="variation file" help="VCF format supported only" />
		</inputs>

	<outputs>
		<data name="output" format="html" />

	</outputs>

</tool>
